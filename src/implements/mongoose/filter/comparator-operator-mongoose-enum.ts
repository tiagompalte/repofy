export enum ComparatorOperatorMongooseEnum {
  EQ = '$eq',
  NEQ = '$ne',
  GT = '$gt',
  GTE = '$gte',
  LT = '$lt',
  LTE = '$lte',
  IN = '$in',
  NIN = '$nin',
  EXISTS = '$exists',
  REGEX = '$regex'
}
